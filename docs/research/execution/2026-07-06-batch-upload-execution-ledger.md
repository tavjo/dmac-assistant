# Batch Upload Skill Execution Ledger

Authority:
- Build plan: `.claude/plans/nextseek-batch-upload-skill-build-2026-06-30.md`
- Design spec: `docs/research/2026-06-30-nextseek-batch-upload-skill-design.md`
- Branch rule: execute on `feat/nextseek-batch-upload-skill` or successor, never `main`

Repository start state:
- Branch: `feat/nextseek-batch-upload-skill`
- Unrelated dirty/untracked files were present before this execution and must not be reverted or staged unless intentionally brought into this work.
- `.codex/`, `.claude/`, and `evidence/` are ignored by `.gitignore`; reviewer findings and live evidence may persist there on disk, but ignored files must not be force-added.

Global execution rules:
- No remote push.
- No destructive git/filesystem actions without explicit permission.
- No ignored file is force-added or force-committed.
- Secret scan staged/intended files before every commit.
- Each wave requires implementation, exact validation, commit, independent alignment review, remediation if needed, and reviewer clean pass before the next wave.

## Wave Status

| Wave | Tasks | Status | Commit(s) | Validation / Evidence | Review Findings |
| --- | --- | --- | --- | --- | --- |
| 0 | W0 live anchor capture | complete after remediation review | `e6f93ac`, `79e33a8` | `evidence/batch-upload-w0/20260706T173555Z/capture_transcript.json` (ignored, persisted on disk); positive verifier passes; fabricated-assays, bogus-request, and tampered-provenance controls reject | rereview pass in `.claude/plans/nextseek-batch-upload-skill-build-2026-06-30-vet/codex-wave-reviews/w0-remediation-review-2026-07-06.md` |
| 1 | T0 deps, fixtures, pin registry | complete after second remediation review | `185a337`, `9eda1d4`, `947d672` | `out/T0.log`, `out/T0.xml`; exact-count `verify_pins_contract.py` => pass; `check_pins.py test_ns_fixtures.py out/T0.xml` => pass; `check_pins.py test_check_pins.py out/T0.xml` => pass | second rereview pass in `.claude/plans/nextseek-batch-upload-skill-build-2026-06-30-vet/codex-wave-reviews/t0-second-remediation-review-2026-07-06.md` |
| 2 | T1 client; T7 cost relay | complete after remediation review | `ec6e840`, `0ae4940` | `out/T1.log`, `out/T1.xml`: T1 client tests 21 passed, `_batch_upload_client` coverage 99%, pin check pass; `out/T7.log`, `out/T7.xml`: explicit non-quarantined WebSocket regression set plus cost relay tests 91 passed, cost-relay and dispatch pin checks pass | rereview pass in `.claude/plans/nextseek-batch-upload-skill-build-2026-06-30-vet/codex-wave-reviews/w2-remediation-review-2026-07-06.md` |
| 3 | T2 payload; T5 extract | complete after remediation review | `635aef3`, `48ab9ac` | `out/T2.log`, `out/T2.xml`: T2 payload tests 17 passed, `_batch_upload_payload` coverage 98%, pin check pass; `out/T5.log`, `out/T5.xml`: T5 extract tests 13 passed, `_batch_upload_extract` coverage 100%, pin check pass | rereview pass in `.claude/plans/nextseek-batch-upload-skill-build-2026-06-30-vet/codex-wave-reviews/w3-remediation-review-2026-07-06.md` |
| 4 | T3 runner/hard gate/resolution | complete after fourth remediation review | `a49ce68`, `830e857`, `d9e754e`, `ea32aff`, `821dfa9`, `5a849be`, `0dc106f` | `out/T3.log`, `out/T3.xml`: T3 runner tests 27 passed, `_batch_upload_runner` coverage 98%, pin check pass; `out/T3-pin-selftest.log`: `test_check_pins.py` 9 passed; edited shell wrappers pass `sh -n`; deleted-flow token scan over `build_context/plugins/nextseek/bin` and runner tests is clean | fourth rereview pass in `.claude/plans/nextseek-batch-upload-skill-build-2026-06-30-vet/codex-wave-reviews/w4-fourth-remediation-review-2026-07-07.md`; residual test-shape debt documented but no blocking T3 data-safety issue |
| 5 | T4 shims; T6 skill contract | complete after remediation review | `9f5d97a`, `10dc8ec`, `d3b805c`, `4336086` | `out/T4.log`, `out/T4.xml`: T4 shim tests 3 passed, pin check pass; `bash -n` and `shellcheck -x` pass on batch-upload shims; `out/T6.log`, `out/T6.xml`: T6 skill contract tests 3 passed | remediation rereview pass in `.claude/plans/nextseek-batch-upload-skill-build-2026-06-30-vet/codex-wave-reviews/w5-remediation-review-2026-07-07.md`; residual scanner-lightweight caveat documented but no blocking T4/T6 issue |
| 6 | T8 $0 E2E; T9 image smoke | complete after remediation review | `1af4cf7`, `568a0ea`, `d627555`, `73d69b6`, `cd14865`, `5c7ece2` | `out/T8.log`, `out/T8.xml`: T8 tests 5 passed, `tools.e2e.run_batch_upload_e2e` coverage 96%, pin check pass; remediated tests require two distinct Bedrock result frames, exact update assay set `{252,306,351}`, and broader AST literal-cost mutants; `out/T9.log`, `out/T9.expected_manifest.json`: clean detached worktree at `cd14865`, `make image-build` succeeded for `dmac-assistant:poc`, T9 smoke passed with 41 HEAD-anchored plugin files, shim dispatch checks, in-image dependency checks, `claude plugin validate /app/plugins/nextseek` pass, and source compile checks | remediation rereview pass in `.claude/plans/nextseek-batch-upload-skill-build-2026-06-30-vet/codex-wave-reviews/w6-remediation-review-2026-07-07.md`; residual docs-ingestion warning is warn-only and outside T9; T9 used `/private/tmp/dmac-t9-clean` because the main worktree has unrelated dirty plugin context files and the image parity check must compare against committed HEAD |
| 7 | T9.5 live-fidelity probe | complete: verifier PASS after server-side advanced_search fix; independent review PASS | `bd345c2`, `b79a243`, `2835392`, `df6020e`, `b643ae4` | `out/T9_5-verifier.log`, `out/T9_5-verifier.xml`: hermetic verifier self-tests 34 passed, `tools.e2e.verify_live_fidelity` coverage 96.48% (re-confirmed 2026-07-07 by an independent rerun with outputs directed outside `out/`); FIRST owner-approved live probe (`evidence/batch-upload-e2e/20260707T155647Z/live_fidelity_probe.json`) HELD: `out/T9_5-live-verify.log` failed with `breadth probe returned fewer than three rows` because the W0 `breadth_probe_uids` bulk `advanced_search` returned `total=0`, `rows=[]`; diagnosed with the owner as a server-side advanced_search cardinality bug (1-2 exact UID terms returned matching rows, >=3 terms returned zero) — owner fixed it server-side and confirmed the deploy live via SwaggerUI on 2026-07-07; SECOND owner-authorized rerun (`tools/e2e/probe_live_fidelity.py --env-file .env.dev`, log `out/T9_5-live-probe-rerun-20260707T181717Z.log`) wrote `evidence/batch-upload-e2e/20260707T181802Z/live_fidelity_probe.json` and the independent verifier exited 0: `{"auth_set": [252, 351], "breadth_rows": 10, "ok": true, "resolved": [252, 351], "uid": "D.IMG-230913ENG-1757-PUB"}` (`out/T9_5-live-verify-rerun-20260707T181717Z.log`; the held-run log kept intact) | independent cold-context review PASS ("in letter and in spirit", verdict PROCEED) in `.claude/plans/nextseek-batch-upload-skill-build-2026-06-30-vet/codex-wave-reviews/w7-review-2026-07-07.md`; reviewer independently re-ran the verifier on BOTH transcripts (pass exit 0 / held exit 1) and the 34-test hermetic suite; four minor non-blocking residuals documented in the review (bounded ~47-call assay-map pagination in the runner; single-page `page_size=1000` assays-map would silently truncate above 1000 assays — forward-looking; owner-gating/server-fix facts are operator-attested; held evidence dirs retained for audit) |
| 8 | T10 paid E2E | complete: PASS (owner-authorized 2026-07-07), independent review PASS "in letter and in spirit" (`.claude/plans/nextseek-batch-upload-skill-build-2026-06-30-vet/codex-wave-reviews/w8-review-2026-07-07.md` — reviewer re-ran the verifier on all three bundles, re-ran the 115-test suite, traced promote-only-after-valid, confirmed genuinely-live; five non-blocking owner-disclosed spirit notes, chiefly create-path-only paid coverage with the assay-wipe seam covered by T9.5) | `a1905d4`, `d600e43`, `2cc067c`, `f91b7ad`, `ccead73` (harness; built TDD + 3 cold adversarial re-vet rounds, final round hardened by an independent canonical hardener per owner directive) | Durable harness: `tools/e2e/run_batch_upload_live_e2e.py` (bridge/WS driver, $5 policy-capped SpendLedger + CC `--max-budget-usd` prevention) + `tools/e2e/batch_upload_live_evidence.py` (pure verdict/cost logic) + `tools/e2e/verify_batch_upload_live.py` ($0 verifier recomputing from the transcript) + 115 hermetic tests 100%/99% coverage (`out/T10-core.log`/`.xml`). Bedrock proxy deconflicted from the running NExtSEEK stack as `dmac-bedrock-proxy-t10` (image `dmac-bedrock-proxy:poc-t10`, network `dmac-nextseek-net`, alias `bedrock-proxy`). FOUR attempts, cumulative **$1.598** of the $5 session cap (each cost authoritative from the CC result frame): (1) $0 — driver crashed pre-spawn on missing bundle dir (fixed: mkdirs mirroring `run_router_e2e._async_main`); (2) $0.405 RED — agent used chat-ops `nextseek-api-read` for project lookup (needs the NS sidecar, absent by design) and correctly fail-stopped (`evidence/batch-upload-e2e/20260707T203043Z/`); (3) $0.580 RED — agent derived project id 6 from the STALE baked `projects_db.json`, live dev grants only id 1, and correctly refused to auto-select (`.../20260707T203532Z/`); (4) **$0.613 PASS** — with the curator id↔name confirmation in the request: route `container_cc`, shims `project-resolve → sampletype-attrs → build-payload → validate-upload` genuinely invoked (sample-search/assay-resolve legitimately skipped for a pure create row — green-but-flagged review note), workbook built via the SHIPPED path, validated by the REAL `batch-upload/validate/` endpoint, promoted to `/data/scratch`, and published by the bridge copier (`.../20260707T203939Z/`: `live_e2e_transcript.json`, `per_turn_summary.json`, `ledger.jsonl` + reconciliation, `verify_result.json`; workbook sha-identical scratch↔published; contents match DD-05: blank-UID create row, canonical json_metadata, sibling assay_ids column). Independent verifier exit 0, ledger reconciles exactly to the authoritative $0.61316325, within the $5 policy cap | LIVE-DISCOVERED SKILL FINDINGS (follow-ups, not harness defects): (a) `nextseek-project-resolve` emits accessible project IDS ONLY, so SKILL step 1's "resolve by name, never typed id" is unimplementable with the skill's own six tools — the curator's id↔name confirmation is the de-facto required input; fix = emit `{id,title}` pairs or amend SKILL step 1; (b) the image-baked `projects_db.json` is stale vs live (PUBLISHED=6 vs accessible {1}) and SKILL doesn't mark it non-authoritative; (c) SKILL step-1 wording ("look up accessible projects through the API") steers agents to the sidecar-dependent chat-ops path. POSITIVE live evidence: the skill FAILED CLOSED correctly in both red attempts (no fabricated ids, no auto-select, clean curator escalation). Pending: independent Wave 8 review |

| 8R | T10 live-finding remediation (owner directive: live-test findings are release blockers, fix before merge) | complete: fixes built TDD, image rebuilt + smoked, proof runs green; independent review returned PARTIAL (H1: the shim itself still rejected `--name` — the module-only fix let the agent bypass the shim), remediated in `57f4bb2` (shim forwards `--name`, two-step unconfirmed flow allowed, id-or-name required; 4 red-first shim tests; 108-test surface green; pinned argv order preserved; image rebuilt + T9 smoke PASS), then RE-PROVEN through the shim: `.../20260707T231422Z/` all_pass=true **$0.874748**, transcript shows `./nextseek-project-resolve --name 'Published Data' --confirmed` (the SHIM, no bypass; detected as an invoked shim), verifier exit 0 ledger==authoritative; cumulative session spend **$3.865 of $5.00** | `b9f729a`, `57f4bb2` | Fixes: `project-resolve` now resolves `--name` against the LIVE `/projects/` titles (fail-closed on unknown/ambiguous/id-mismatch, surfaces accessible `{id,title}`; pinned token contract unchanged, additive fields only) + SKILL step 1 rewritten (live-answer-only resolution; `context/` catalogs explicitly non-authoritative; no other tool for project lookup; all pinned contract phrases kept). 7 new red-first hermetic tests; runner 34 passed @98%; skill-contract 3 passed; full batch-upload surface 101 passed; pin checks green; `verify_pins_contract` ok 68. Image rebuilt from clean detached worktree `/private/tmp/dmac-t10-clean` @ `b9f729a` (gitignored `build_context/docs/nextseek-api/` copied in; prior image auto-retagged `:poc-prev`); T9 smoke re-run PASS (41 HEAD-anchored files, `plugin_validate=passed`). PROOF RUNS (name-only query, NO id, NO tool coaching): first attempt $0.599 HELD — dev returned 502 mid-run then timed out from the host (correct agent behavior: used `--name 'Published Data' --confirmed` unprompted, refused the stale catalog, bounded diagnostic retry, clean stop; `evidence/batch-upload-e2e/20260707T223832Z/`); after owner confirmed dev back up, rerun **$0.793 all_pass=true** (`.../20260707T224600Z/`): live name→id resolution → confirmed token accepted by the hard gate → TIS schema → staged build → real validate (`structure,name_check,dag`) → delivered `/data/scratch/demo/pubdata-tis/payload_flat.xlsx`, sha-identical scratch↔published, verifier exit 0 with ledger == authoritative $0.79336575. Cumulative session spend **$2.990 of $5.00** | Documented detector limitation (conservative, no forgery direction): the agent invoked project-resolve via `python .../_batch_upload_runner.py project-resolve ...` (direct module) which the invocation detector counts only by shim basename → surfaced as a green-but-flagged review note; inspection of the raw transcript confirms the genuine invocation. Pending: independent remediation review |

## Review Prompt Template

For each completed wave, dispatch an independent reviewer with:

> Wave `<WAVE_ID>` implementation is complete. Ultra think, then evaluate the actual outcome against the original batch upload plan and each task's stated success conditions for this wave. Mark each task pass, partial, or fail, and explain why. Identify any success conditions that were satisfied technically but not in spirit. Check for forbidden shortcuts, weakened tests, skipped pins, fabricated evidence, ignored-file staging, missing secret scans, and drift from the original plan. Persist your findings on disk under `.claude/plans/nextseek-batch-upload-skill-build-2026-06-30-vet/codex-wave-reviews/`.

Final reviewer prompt:

> Execution is complete. Ultra think, then evaluate the actual outcome against the original spec and each task's stated success conditions. For each task : mark it pass, partial, or fail, and explain why. Identify any success conditions that were satisfied technically but not in spirit. Produce a final verdict on whether my original will was carried out, and flag any residual debt — things that technically work but shouldn't be left as-is.
